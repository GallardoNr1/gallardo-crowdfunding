---
// import '../styles/global.css';
import Header from './Header.astro';
import Footer from './Footer.astro';
import ContributionModal from '@/components/ContributionModal.astro';

export interface Props {
  title: string;
  description?: string;
  subtitle?: string;
}

const {
  title,
  description = 'Plataforma familiar de crowdfundings Gallardo',
  subtitle,
} = Astro.props;
const url = new URL(Astro.request.url);
const id = url.searchParams.get('id');
const customPaymentMethods = [
  {
    id: 'cash',
    name: 'En efectivo',
    icon: '🤝',
    description: 'Pago en mano',
    enabled: true,
  },
  {
    id: 'bizum',
    name: 'Bizum',
    icon: '📱',
    description: 'Pago móvil rápido',
    enabled: true,
  },
];
const emojiOptions: { value: string; label: string }[] = [
  { value: '🧬', label: 'Códice Viviente' },
  { value: '🤖', label: 'Centinela Digital' },
  { value: '🛡️', label: 'Guardían del Sistema' },
  { value: '⚡', label: 'Nexo Eléctrico' },
  { value: '🛰️', label: 'Explorador Estelar' },
  { value: '🧠', label: 'Mente Avanzada' },
  { value: '🔮', label: 'Oráculo de Datos' },
  { value: '🦾', label: 'Fuerza Mecánica' },
  { value: '📡', label: 'Analista Cósmico' },
  { value: '🌌', label: 'Viajero del Vacío' },
];
---

<!doctype html>
<html lang='es'>
  <head>
    <meta charset='UTF-8' />
    <meta
      name='viewport'
      content='width=device-width, initial-scale=1.0'
    />
    <meta
      name='description'
      content={description}
    />
    <title>{title}</title>

    <link
      rel='preconnect'
      href='https://fonts.googleapis.com'
    />
    <link
      rel='preconnect'
      href='https://fonts.gstatic.com'
      crossorigin
    />
    <link
      href='https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap'
      rel='stylesheet'
    />
    <link
      rel='stylesheet'
      href='/styles/global.css'
    />
  </head>
  <body>
    <Header
      title={title}
      subtitle={subtitle}
    />
    <main class='main-content'>
      <slot />
    </main>
    <slot name='footer' />
    <ContributionModal
      paymentMethods={customPaymentMethods}
      currency='€'
      modalTitle='Apoya el proyecto familiar'
      modalIcon='🧡'
      modalSubtitle='Ayuda a la familia a crear recuerdos inolvidables'
      summaryTitle='Resumen de tu aporte'
      formTitle='Tus datos'
      paymentTitle='Selecciona método'
      successTitle='¡Gracias por tu apoyo!'
      successMessage='Hemos registrado tu contribución correctamente.'
      cancelButtonText='Cerrar'
      confirmButtonText='Confirmar y Registrar'
      bizumNumber='648 029 265'
      bizumConcept='Contribución familiar + Tu Nombre'
      totalLabel='Total a contribuir:'
      emojiOptions={emojiOptions}
      projectId={id ? id : ''}
    />
  </body>
</html>
