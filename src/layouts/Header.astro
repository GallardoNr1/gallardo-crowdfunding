---
export interface Props {
  title?: string;
  subtitle?: string;
}

const {
  title = 'Gallardo Crowdfunding',
  subtitle = 'Elige un proyecto y únete a hacerlo realidad',
} = Astro.props;
---

<dic class='header-bg'>
  <div class='header'>
    <div class='left'>
      <a
        class='home-link'
        href='/'
        title='Ir a la página principal'
      >
        <span class='logo'>
          <span class='logo-inner'>GC</span>
        </span>
      </a>
    </div>

    <div class='center'>
      <h1 class='title'>{title}</h1>
      <p class='subtitle'>{subtitle}</p>
    </div>
  </div>
</dic>

<style>
  .header-bg {
    background: var(--color-header-bg);
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
  }
  .header {
    display: flex;
    align-items: center;
    padding: 35px 20px;
    margin-bottom: 40px;
    gap: 20px;
    width: 100%;
    max-width: 1200px;
  }

  .left {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .center {
    flex: 1;
    text-align: center;
  }

  /* === LOGO PRO === */
  .logo {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 70px;
    height: 70px;
    border-radius: 50%;
    background: radial-gradient(circle at 30% 30%, #4b5563, var(--color-header-bg));
    border: 3px solid #6b7280;
    box-shadow:
      0 0 10px rgba(255, 255, 255, 0.05),
      inset 0 0 10px rgba(0, 0, 0, 0.4);
    transition: var(--transition-normal);
  }

  .logo-inner {
    font-size: 1.5rem;
    font-weight: 800;
    color: #f9fafb;
    letter-spacing: -1px;
  }

  .home-link:hover .logo {
    border-color: #9ca3af;
    transform: scale(1.06) rotate(-2deg);
    box-shadow:
      0 0 16px rgba(255, 255, 255, 0.12),
      inset 0 0 14px rgba(0, 0, 0, 0.5);
  }

  .title {
    font-family: var(--font-body);
    font-size: var(--text-3xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-header-text);
    margin: 0;
  }

  .subtitle {
    font-size: var(--text-md);
    color: var(--color-header-subtitle);
    margin-top: 6px;
  }

  @media (max-width: 768px) {
    .header {
      flex-direction: column;
      padding: 25px 20px;
    }

    .center {
      text-align: center;
    }

    .logo {
      width: 60px;
      height: 60px;
    }
  }
</style>
