<div class='floating-elements'>
  <div
    class='floating-brick'
    style='left: 10%; animation-delay: 0s;'
  >
    🧱
  </div>
  <div
    class='floating-brick'
    style='left: 20%; animation-delay: 1s;'
  >
    🟥
  </div>
  <div
    class='floating-brick'
    style='left: 30%; animation-delay: 2s;'
  >
    🟦
  </div>
  <div
    class='floating-brick'
    style='left: 40%; animation-delay: 3s;'
  >
    🟩
  </div>
  <div
    class='floating-brick'
    style='left: 50%; animation-delay: 4s;'
  >
    🟨
  </div>
  <div
    class='floating-brick'
    style='left: 60%; animation-delay: 5s;'
  >
    🧱
  </div>
  <div
    class='floating-brick'
    style='left: 70%; animation-delay: 1.5s;'
  >
    🟥
  </div>
  <div
    class='floating-brick'
    style='left: 80%; animation-delay: 2.5s;'
  >
    🟦
  </div>
  <div
    class='floating-brick'
    style='left: 90%; animation-delay: 3.5s;'
  >
    🟩
  </div>
</div>

<style>
  /* Contenedor de elementos flotantes */
  .floating-elements {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
    overflow: hidden;
    z-index: -1; /* Detrás del contenido principal */
  }

  /* Ladrillos flotantes individuales */
  .floating-brick {
    position: absolute;
    top: -50px; /* Comienzan arriba de la pantalla */
    font-size: 2rem;
    animation: floatDown 15s linear infinite;
    opacity: 0.7;
    transition: opacity 0.3s ease;
  }

  /* Animación principal de caída */
  @keyframes floatDown {
    0% {
      top: -50px;
      transform: rotate(0deg) scale(1);
      opacity: 0;
    }
    10% {
      opacity: 0.7;
    }
    90% {
      opacity: 0.7;
    }
    100% {
      top: calc(100vh + 50px);
      transform: rotate(360deg) scale(0.8);
      opacity: 0;
    }
  }

  /* Variaciones de animación para hacer más dinámico */
  .floating-brick:nth-child(odd) {
    animation: floatDownWiggle 18s linear infinite;
  }

  .floating-brick:nth-child(3n) {
    animation: floatDownSpin 12s linear infinite;
  }

  .floating-brick:nth-child(4n) {
    animation: floatDownBounce 20s linear infinite;
  }

  /* Animación con movimiento lateral */
  @keyframes floatDownWiggle {
    0% {
      top: -50px;
      transform: translateX(0) rotate(0deg);
      opacity: 0;
    }
    10% {
      opacity: 0.7;
    }
    25% {
      transform: translateX(20px) rotate(90deg);
    }
    50% {
      transform: translateX(-20px) rotate(180deg);
    }
    75% {
      transform: translateX(15px) rotate(270deg);
    }
    90% {
      opacity: 0.7;
    }
    100% {
      top: calc(100vh + 50px);
      transform: translateX(0) rotate(360deg);
      opacity: 0;
    }
  }

  /* Animación con rotación más rápida */
  @keyframes floatDownSpin {
    0% {
      top: -50px;
      transform: rotate(0deg) scale(1);
      opacity: 0;
    }
    10% {
      opacity: 0.8;
    }
    50% {
      transform: rotate(720deg) scale(1.2);
    }
    90% {
      opacity: 0.8;
    }
    100% {
      top: calc(100vh + 50px);
      transform: rotate(1440deg) scale(0.6);
      opacity: 0;
    }
  }

  /* Animación con efecto rebote */
  @keyframes floatDownBounce {
    0% {
      top: -50px;
      transform: scale(1);
      opacity: 0;
    }
    10% {
      opacity: 0.6;
    }
    20%,
    40%,
    60%,
    80% {
      transform: scale(1.1);
    }
    30%,
    50%,
    70% {
      transform: scale(0.9);
    }
    90% {
      opacity: 0.6;
    }
    100% {
      top: calc(100vh + 50px);
      transform: scale(0.8);
      opacity: 0;
    }
  }

  /* Efectos hover para interactividad */
  .floating-elements:hover .floating-brick {
    animation-play-state: paused;
    opacity: 1;
    transform: scale(1.3);
  }

  /* Variaciones de tamaño */
  .floating-brick:nth-child(2n) {
    font-size: 1.5rem;
  }

  .floating-brick:nth-child(3n + 1) {
    font-size: 2.5rem;
  }

  .floating-brick:nth-child(5n) {
    font-size: 1.8rem;
  }

  /* Efectos de brillo para algunos ladrillos */
  .floating-brick:nth-child(7n) {
    filter: drop-shadow(0 0 10px rgba(255, 215, 0, 0.5));
  }

  .floating-brick:nth-child(11n) {
    filter: drop-shadow(0 0 8px rgba(255, 0, 128, 0.4));
  }

  /* Versión más sutil para fondos */
  .floating-elements.subtle .floating-brick {
    opacity: 0.3;
    font-size: 1.2rem;
    animation-duration: 25s;
  }

  /* Versión más intensa para celebraciones */
  .floating-elements.celebration .floating-brick {
    opacity: 0.9;
    font-size: 3rem;
    animation-duration: 8s;
  }

  .floating-elements.celebration .floating-brick:nth-child(even) {
    animation: floatDownSpin 6s linear infinite;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .floating-brick {
      font-size: 1.5rem;
    }

    .floating-brick:nth-child(3n + 1) {
      font-size: 2rem;
    }

    .floating-elements.celebration .floating-brick {
      font-size: 2rem;
    }
  }

  @media (max-width: 480px) {
    .floating-brick {
      font-size: 1.2rem;
    }

    .floating-elements.celebration .floating-brick {
      font-size: 1.8rem;
    }
  }

  /* Pausa animaciones si el usuario prefiere movimiento reducido */
  @media (prefers-reduced-motion: reduce) {
    .floating-brick {
      animation: none;
      opacity: 0.2;
      position: static;
      display: inline-block;
      margin: 0 5px;
    }

    .floating-elements {
      position: static;
      text-align: center;
      padding: 20px;
    }
  }

  /* Efectos especiales para ciertos emojis */
  .floating-brick:contains('🧱') {
    filter: drop-shadow(2px 2px 4px rgba(0, 0, 0, 0.3));
  }

  /* Gradientes de fondo opcional */
  .floating-elements::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
      45deg,
      transparent 0%,
      rgba(255, 255, 255, 0.02) 25%,
      transparent 50%,
      rgba(255, 255, 255, 0.02) 75%,
      transparent 100%
    );
    pointer-events: none;
    z-index: -1;
  }

  /* Modo nocturno / tema oscuro */
  @media (prefers-color-scheme: dark) {
    .floating-brick {
      opacity: 0.5;
      filter: brightness(0.8);
    }

    .floating-elements::before {
      background: linear-gradient(
        45deg,
        transparent 0%,
        rgba(255, 255, 255, 0.01) 25%,
        transparent 50%,
        rgba(255, 255, 255, 0.01) 75%,
        transparent 100%
      );
    }
  }

  /* Animación especial para cuando aparecen nuevos ladrillos */
  .floating-brick.new-brick {
    animation:
      spawnIn 0.5s ease-out,
      floatDown 15s linear 0.5s infinite;
  }

  @keyframes spawnIn {
    0% {
      transform: scale(0) rotate(0deg);
      opacity: 0;
    }
    50% {
      transform: scale(1.3) rotate(180deg);
      opacity: 1;
    }
    100% {
      transform: scale(1) rotate(360deg);
      opacity: 0.7;
    }
  }
</style>
